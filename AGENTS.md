# 工作约定

- 用户要求：完成后的项目统一存放在 `/Users/zhengwang/github_repos/` 下，以独立 Git 仓库形式管理；本仓库现位于 `/Users/zhengwang/github_repos/worker-ledger/`。
- 每次完成一轮修改后，应提交到 `main` 分支并与 GitHub 远程保持同步（`git add -A` → `commit` → `push`）。
- 如果 GitHub 认证或远程仓库不可用，不要静默跳过：说明阻塞点并等待用户恢复认证/提供远程地址。
